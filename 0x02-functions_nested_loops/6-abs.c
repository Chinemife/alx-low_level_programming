#include "main.h"

/**
 * _abs - prints absolute figures
 * @r: enters the required number
 * Return: r if positive or -r if negative
 */

int _abs(int r)
{
if (r >= 0)
{
	return (r);
}
else
{
	return (-r);
}
}
